<textarea class="input-textarea<% if (errors[textarea.name]) { %> errored<% } %>" <%= textarea.required ? "required" : "" %> name="<%= textarea.name %>">
<% if (!textarea.blockValue && (formData[textarea.name] || errors[textarea.name]?.value)) { %><%= formData[textarea.name] ? formData[textarea.name] : errors[textarea.name].value %><% } %>
</textarea>
<% if (errors[textarea.name]) { %>
    <p class="input-textarea-error"><%= errors[textarea.name].msg %></p>
<% } %>
