<header class="header">
    <div class="header-container">
        
        <div class="header-box">
            <nav class="main-nav">
                <a href="/invites"><%= __("Invites") %> </a>
                <a href="/services"><%= __("Services") %> </a>
                
                <% if (hasPermission(account.role,"MANAGE_USERS")) { %>
                    <a href="/users"><%= __("Users") %> </a>
                <% } %>
                

            </nav>
        </div>

        <div class="header-box account-box">
            <div class="account-profile">
                <img src="/icons/account.svg" alt="">
                <div class="account-info">
                    <p class="username"><%= account.username %></p>
                </div>
            </div>
            <nav class="account-nav">
                <a href="/account"><img src="/icons/settings.svg" alt=""></a>

                <button form="op.logoutForm"><img src="/icons/logout.svg" alt=""></button>

                <form id="op.logoutForm" method="post" action="/logout"><input type="hidden" value="<%= csrfToken %>" name="_csrf"></form>            
            </nav>
        </div>

    </div>
</header>