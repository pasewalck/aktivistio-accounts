<%- include("../../partials/dashboard-header.ejs") %>

<div class="wrapper flex-container">
    <div class="container container-medium-width container-filled-background">

        <h2><%= currentClientId ? __("service.update") : __("service.create") %> </h2>
    
        <form class="form" action="<%= urlUtils.extendUrl(baseUrl, currentClientId ? `services/edit/${currentClientId}/save` : `services/add`).href %>" method="post">
            <fieldset>

                <div class="form-group">
                    <label class="label"><%= __('service.configuration') %></label>
                    <%- include("../../partials/form/input-textarea.ejs",{textarea: {name:"configuration",required:true}}) %> 
                </div>

                <div class="form-group">
                    <label class="label"><%= __('user.admin_password') %></label>
                    <%- include("../../partials/form/input-field.ejs",{field: {name:"adminPassword",type:"password",required:true}}) %> 
                </div>

            </fieldset>

            <input type="hidden" value="<%= csrfToken %>" name="_csrf">
            <div class="form-action-group">
                <button class="button red" type="submit"><%= currentClientId ? __("form.update") : __("form.create") %> </button>
            </div>     

        </form>

    </div>

    <% if (currentClientId) { %>
        <div class="container container-medium-width container-filled-background">

            <h2><%= __("service.delete") %> </h2>
    
            <form class="form" action="<%= urlUtils.extendUrl(baseUrl, `services/edit/${currentClientId}/delete`).href %>" method="post">
                <fieldset>
    
                    <div class="form-group">
                        <label class="label"><%= __('user.admin_password') %></label>
                        <%- include("../../partials/form/input-field.ejs",{field: {name:"deletePermAdminPassword",type:"password",required:true}}) %> 
                    </div>
    
                    <div class="form-group">
                        <label class="label"><%= __('form.confirm_action') %></label>
                        <%- include("../../partials/form/input-checkbox.ejs",{checkbox: {name:"deleteAdminConfirm",msg:__('service.delete_confirm',currentClientId)}}) %>             
                    </div>
    
                </fieldset>
    
                <input type="hidden" value="<%= csrfToken %>" name="_csrf">
                <div class="form-action-group">
                    <button class="button red" type="submit"><%= __("service.delete_forever") %> </button>
                </div>     
    
            </form>
    
        </div>
    <% } %>
    
</div>
